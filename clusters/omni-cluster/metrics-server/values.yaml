args:
  - --kubelet-insecure-tls                                       # Talos kubelet serving cert is self-signed (lab)
  - --kubelet-preferred-address-types=InternalIP,ExternalIP,Hostname
  - --metric-resolution=15s
resources:
  requests: { cpu: 100m, memory: 200Mi }
  limits:   { memory: 300Mi }
