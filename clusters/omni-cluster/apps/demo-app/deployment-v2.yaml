apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-app-v2
  namespace: demo-app
  labels: { app: demo-app }
spec:
  replicas: 1
  selector:
    matchLabels: { app: demo-app, version: v2 }
  template:
    metadata:
      labels: { app: demo-app, version: v2 }
    spec:
      imagePullSecrets:
        - name: ghcr-pull
      containers:
        - name: demo-app
          image: ghcr.io/example-user/demo-app:<sha>
          ports:
            - { name: http, containerPort: 8080 }
          resources:
            requests: { cpu: 50m, memory: 32Mi }
            limits:   { memory: 64Mi }
